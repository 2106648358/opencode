## 1. Update glyph data

- [x] 1.1 Replace the `logo` left and right glyph arrays in `src/cli/logo.ts` with new "EzCode" glyphs (3 left characters + 3 right characters)
- [ ] 1.2 Verify the z character renders correctly in both TUI (shimmer) and CLI (plain-text) paths

## 2. Update wordmark

- [x] 2.1 Replace the `wordmark` array in `src/cli/ui.ts` with the "EzCode" plain-text equivalent

## 3. Verify

- [x] 3.1 Run type checking to confirm no type errors
- [ ] 3.2 Verify the logo renders correctly in the TUI home page
